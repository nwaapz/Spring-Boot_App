spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/products
    username: postgres
    password: 1111
  flyway:
    enabled: true
  thymeleaf:
    cache: false
